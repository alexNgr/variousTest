#include <iostream>

#include "stefan.hpp"

constexpr double pi = calculate_pi();
int main() {

    
    const double radius = 1.5;

	std::cout << "Area: " << calc_area(pi, radius) << '\n';

	std::cout << "Sum: " << sum(3,4) << '\n';

	std::cout << "Sum_multi: " << sum_multi(4, 3, 1, 0) << '\n';
  

	return __linux>__unix;
}